@page "/tour"

<PageTitle>Tour | Sean</PageTitle>

<section class="section">
    <div class="container">
        <div class="section-heading">
            <h2>Upcoming Shows</h2>
            <p>Come hear it live</p>
        </div>

        <RevealSection>
            <div>
                <UpcomingShowDates />
            </div>
        </RevealSection>
    </div>
</section>

<RevealSection Class="section section-alt">
    <div class="container">
        <div class="section-heading">
            <h2>Past Shows</h2>
            <p>Where Sean's been</p>
        </div>
        <div style="text-align:center; margin-top:1rem;">
            <button class="btn toggle-past" @onclick="TogglePastShows">@(showPast ? "Hide" : "Show") Past Shows</button>
        </div>

        @if (showPast)
        {
            <div style="margin-top:1.5rem;">
                <PastShowDates />
            </div>
        }
    </div>
</RevealSection>

@code {
    private bool showPast;

    private void TogglePastShows() => showPast = !showPast;
}
